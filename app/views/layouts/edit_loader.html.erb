<head>

<style>
#register
{
    zIndex: 10000000;
    background-color: black;
    position: absolute;
    left:0;
    right:0;
    top:0;
    bottom:0;
    height:300px;
    width:500px;
}
</style>

<script>
<% if @shape != nil %>
var savedShape = "<%= @shape[:shape] %>";
var shapeID = "<%= @shape[:id] %>";
var authToken = "<%= form_authenticity_token %>";
<%
if (params[:meta] == "1") %>
    var screenShot = true;
<% end %>
<% end %>

function saveShape()
{
    $.post("/save", {id: "<%= @shape[:id] %>", authenticity_token: "<%= form_authenticity_token %>", shape: hashend});
    loadScreenshotStage();
}

</script>
</head>

<%= yield %>
